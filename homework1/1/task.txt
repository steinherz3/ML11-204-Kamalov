k-means
Датасет - ирисы.
1. Найти оптимальное количество кластеров при помощи готовых библиотек (sklearn).
2. Написать самостоятельно алгоритм для оптимального количества кластеров
(п.1) k-means без использования библиотек (но можно пользоваться библиотеками, не связанными с самим алгоритмом - отрисовки, подсчетов и т.д.).
Рисунки выводятся на каждый шаг –
сдвиг центроидов, смена точек своих кластеров.
Сколько шагов, столько рисунков (можно в виде gif).
Точки из разных кластеров разными цветами.



















# Этот код выполняет кластеризацию данных с использованием алгоритма k-means.
# Он загружает набор данных ирисов, затем выполняет цикл для различных значений k (количество кластеров),
# применяя модель k-means и сохраняя сумму квадратов расстояний от каждой точки до её ближайшего центроида
# (инерцию) для каждого значения k. После этого он визуализирует эти значения инерции в виде графика,
# который называется "методом локтя".


# Этот код выполняет кластеризацию данных с использованием алгоритма k-means.
# Сначала он загружает набор данных ирисов и затем инициализирует центроиды случайным образом.
# Далее он выполняет цикл максимум 100 раз, в котором для каждой точки вычисляет расстояние до
# каждого центроида и присваивает ей метку кластера. Затем он обновляет положение каждого центроида
# как среднего значения всех точек в его кластере. Если центроиды не изменились, цикл прерывается.
# Наконец, код визуализирует процесс кластеризации, показывая начальные центроиды и точки данных,
# распределенные по кластерам после каждого шага алгоритма.